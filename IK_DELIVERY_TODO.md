# IK 正式交付差距 TODO

本文档用于维护从当前 RViz 评审 Demo 到正式项目交付版本的差距清单。状态含义：

- `[x]` 已完成并在当前代码中可复现。
- `[~]` 已有原型，但还不满足正式交付口径。
- `[ ]` 未开始或等待甲方输入。

## 总体状态

当前代码处于“技术可行性原型 + 客户评审 Demo”阶段。已证明：

- 可以解析当前 ELF3 左右 7 轴机械臂 URDF。
- 可以接收 `base_link` 坐标系下左右臂目标位姿。
- 可以输出双臂关节命令，并在 RViz 中展示 IK、回零、握手起手式。
- 可以循环展示“任意姿态 -> 回零”和“任意姿态 -> 左臂归零、右臂握手起手式”。

尚未达到正式交付口径的关键差距：

- 未接入 `withoutarm.onnx` state 的真实控制链路。
- 未提供正式 ROS2 Action / 自定义 Service 接口。
- 未完成工程级多解选择、奇异检测、碰撞保护、速度/加速度约束。
- 未适配甲方后续指定的 2 个新增型号。
- 未形成完整测试报告和多型号回归验证。

## 1. 机器人模型解析与运动链建模

状态：`[~]`

已完成：

- [x] 当前 ELF3 左臂 7 轴 URDF 链解析。
- [x] 当前 ELF3 右臂 7 轴 URDF 链解析。
- [x] 左右臂关节名称、零位、演示姿态配置。
- [x] TCP 偏置参数 `tcp_offset`。
- [x] 基础 FK/IK 能力依赖 `ikpy` chain。
- [x] 建立正式模型配置目录 `src/elf3_arm_ik_baselink/config/models/`。
- [x] 新增基准 ELF3 模型配置 `elf3.yaml`。

待完成：

- [ ] 从模型配置加载当前 ELF3 参数，减少硬编码。
- [ ] 等待甲方书面确认 2 个新增型号名称、URDF、mesh、版本号。
- [ ] 适配新增型号 1 的左右 7 轴链路。
- [ ] 适配新增型号 2 的左右 7 轴链路。
- [ ] 形成每个型号的模型校核记录。

验收标准：

- 每个型号均可单独加载并输出左右臂 FK/IK。
- 关节方向、关节限位、零位、TCP 偏置有配置文件和校核说明。

## 2. 6D 末端逆解算法核心

状态：`[~]`

已完成：

- [x] 数值 IK 原型支持位置 + 姿态输入。
- [x] 支持左右臂。
- [x] 支持 TCP 偏置。
- [x] IK 初值使用当前关节状态，具备基本连续性。
- [x] IKPy 基于 URDF bounds 处理关节限位。

待完成：

- [ ] 增加 IK 结果误差计算：位置误差、姿态误差。
- [ ] 增加不可达目标明确状态，不输出未经检查的关节解。
- [ ] 增加接近奇异位形检测。
- [ ] 增加多解采样与评分。
- [ ] 评分项包含：当前关节连续性、限位裕量、碰撞风险、可执行性。
- [ ] 增加单次 IK 求解耗时统计，不再只统计服务返回耗时。
- [ ] 建立 20ms 目标的基准测试。

验收标准：

- 对可达目标输出可执行关节解和误差。
- 对不可达、奇异、碰撞风险目标输出失败状态和原因。
- 典型工况 IK 耗时满足项目确认指标。

## 3. 连续轨迹生成与中断归零

状态：`[~]`

已完成：

- [x] 关节空间平滑插值轨迹。
- [x] 任意演示姿态到回零。
- [x] 任意演示姿态到左臂归零、右臂握手起手式。
- [x] 基于当前 joint state 作为轨迹起点。

待完成：

- [ ] 使用正式 `trajectory_msgs/JointTrajectory` 输出。
- [ ] 增加速度约束。
- [ ] 增加加速度约束。
- [ ] 增加控制周期参数化。
- [ ] 使用 ROS2 Action 支持动作执行中的 cancel / stop。
- [ ] 外部停止后读取停止时当前关节状态，规划返回甲方确认的安全预备位。
- [ ] 明确安全预备位由甲方确认并写入配置。

验收标准：

- 上层可中断动作。
- 任意中断点可重新规划回安全预备位。
- 轨迹满足位置、速度、加速度、控制周期约束。

## 4. 自碰撞保护策略

状态：`[ ]`

已完成：

- [x] 原型中有左右 TCP 最小距离保护。

待完成：

- [ ] 建立躯干、头部、左右上臂、左右前臂、腕部的简化碰撞代理体。
- [ ] 实现胶囊体 / 球体 / 盒体距离检查。
- [ ] 输出碰撞状态、风险部位和不可执行原因。
- [ ] 将碰撞风险接入 IK 选解评分。
- [ ] 将碰撞风险接入轨迹采样检查。
- [ ] 结合甲方现场设备确认安全边界和代理体参数。

验收标准：

- 机械臂与躯干、头部、对侧机械臂主要碰撞风险可实时检测。
- 碰撞或近碰撞目标不会输出可执行轨迹。

## 5. ROS2 接口封装

状态：`[~]`

已完成：

- [x] 输入 topic：`arm_ik/left_target`、`arm_ik/right_target`。
- [x] 输出 topic：`pico_control_joint_commands`。
- [x] Demo 服务：`go_home`、`go_handshake_ready`、`go_demo_pose_a`、`go_demo_pose_b`。

待完成：

- [ ] 定义正式 `SolveArmIk` Service 或 Action。
- [ ] 请求字段包含：机器人型号、左右臂、当前关节状态、目标 6D 位姿、TCP 配置、约束参数。
- [ ] 响应字段包含：求解状态、关节解或 JointTrajectory、位置误差、姿态误差、求解耗时、碰撞状态、失败原因。
- [ ] 定义正式 `PlanArmTrajectory` Action，用于长动作和中断。
- [ ] 接入 `withoutarm.onnx` state 的最终控制输出合并点。

验收标准：

- 上层控制系统可通过稳定接口调用 IK / 轨迹规划。
- 每次调用均有结构化状态和失败原因。

## 6. 可视化评审 Demo

状态：`[~]`

已完成：

- [x] RViz 加载 URDF 模型。
- [x] 循环展示任意姿态到回零。
- [x] 循环展示任意姿态到握手起手式。
- [x] 字幕显示当前步骤。
- [x] 字幕显示服务触发到轨迹启动返回耗时。

待完成：

- [ ] 型号选择。
- [ ] 左右臂选择。
- [ ] 拖动或交互设置末端目标位姿。
- [ ] 展示 IK 位置误差和姿态误差。
- [ ] 展示真实单次 IK 求解耗时。
- [ ] 展示碰撞状态和风险部位。
- [ ] 演示外部中断停止和返回安全位。

验收标准：

- 客户能在本地选择型号、手臂、目标位姿，并看到完整 IK / 碰撞 / 轨迹状态。

## 7. 多型号适配

状态：`[ ]`

已完成：

- [x] 当前 ELF3 基准模型原型适配。

待完成：

- [ ] 获取新增型号 1 的正式名称、URDF、mesh、关节配置、TCP 配置。
- [ ] 获取新增型号 2 的正式名称、URDF、mesh、关节配置、TCP 配置。
- [ ] 新增型号 1 运动链适配。
- [ ] 新增型号 2 运动链适配。
- [ ] 新增型号 1 碰撞代理体适配。
- [ ] 新增型号 2 碰撞代理体适配。
- [ ] 新增型号 1 ROS2 配置和典型工况测试。
- [ ] 新增型号 2 ROS2 配置和典型工况测试。
- [ ] 输出多型号适配说明。

验收标准：

- 基准型号 + 2 个新增型号均通过同一接口调用。
- 每个型号有独立配置、测试结果和适配说明。

## 8. 测试验证与交付文档

状态：`[~]`

已完成：

- [x] 基础数学工具单元测试。
- [x] Ubuntu VM 上 `colcon build` 验证。
- [x] RViz Demo 人工验证。
- [x] 初版部署与使用说明。

待完成：

- [ ] 功能测试用例。
- [ ] 典型工况测试用例。
- [ ] 边界与异常测试用例。
- [ ] 误差和耗时记录脚本。
- [ ] 多型号回归测试。
- [ ] ROS2 接口测试。
- [ ] 连续轨迹和中断归零测试。
- [ ] 自碰撞状态测试。
- [ ] 可复现测试方法。
- [ ] 测试报告。
- [ ] 正式接口说明。
- [ ] 正式部署与使用文档。

验收标准：

- 测试可以在指定 Ubuntu/ROS2 环境复现。
- 交付文档能支持甲方独立部署、调用和验收。

## 当前执行顺序

1. `[已完成]` 建立正式模型配置目录和基准 ELF3 模型配置。
2. `[进行中]` 补 IK 结果误差、失败状态和真实 IK 耗时。
3. `[待办]` 定义正式 ROS2 Service / Action 接口。
4. `[待办]` 增加简化碰撞代理体和碰撞状态输出。
5. `[待办]` 完成中断停止和安全预备位返回。
6. `[待办]` 接入 `withoutarm.onnx` state 控制链路。
7. `[待办]` 等甲方提供 2 个新增型号后做多型号适配。
8. `[待办]` 补测试报告和正式交付文档。
