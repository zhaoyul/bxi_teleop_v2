# IK 正式交付差距 TODO

本文档用于维护从当前 RViz 评审 Demo 到正式项目交付版本的差距清单。状态含义：

- `[x]` 已完成并在当前代码中可复现。
- `[~]` 已有原型，但还不满足正式交付口径。
- `[ ]` 未开始或等待甲方输入。

## 总体状态

当前代码处于“技术可行性原型 + 客户评审 Demo”阶段。已证明：

- 可以解析当前 ELF3 左右 7 轴机械臂 URDF。
- 可以接收 `base_link` 坐标系下左右臂目标位姿。
- 可以输出双臂关节命令，并在 RViz 中展示 IK、回零、握手起手式。
- 可以循环展示“任意姿态 -> 回零”和“任意姿态 -> 左臂归零、右臂握手起手式”。

尚未达到正式交付口径的关键差距：

- 未接入 `withoutarm.onnx` state 的真实控制链路。
- 未完成工程级多解选择和碰撞保护。
- 未适配甲方后续指定的 2 个新增型号。
- 未形成完整测试报告和多型号回归验证。

## 1. 机器人模型解析与运动链建模

状态：`[~]`

已完成：

- [x] 当前 ELF3 左臂 7 轴 URDF 链解析。
- [x] 当前 ELF3 右臂 7 轴 URDF 链解析。
- [x] 左右臂关节名称、零位、演示姿态配置。
- [x] TCP 偏置参数 `tcp_offset`。
- [x] 基础 FK/IK 能力依赖 `ikpy` chain。
- [x] 建立正式模型配置目录 `src/elf3_arm_ik_baselink/config/models/`。
- [x] 新增基准 ELF3 模型配置 `elf3.yaml`。
- [x] 从完整 ELF3 URDF 校核左右肩相对基座安装偏移，并接入 FK/IK。

待完成：

- [ ] 从模型配置加载当前 ELF3 参数，减少硬编码。
- [ ] 等待甲方书面确认 2 个新增型号名称、URDF、mesh、版本号。
- [ ] 适配新增型号 1 的左右 7 轴链路。
- [ ] 适配新增型号 2 的左右 7 轴链路。
- [ ] 形成每个型号的模型校核记录。

验收标准：

- 每个型号均可单独加载并输出左右臂 FK/IK。
- 关节方向、关节限位、零位、TCP 偏置有配置文件和校核说明。

## 2. 6D 末端逆解算法核心

状态：`[~]`

已完成：

- [x] 数值 IK 原型支持位置 + 姿态输入。
- [x] 支持左右臂。
- [x] 支持 TCP 偏置。
- [x] IK 初值使用当前关节状态，具备基本连续性。
- [x] IKPy 基于 URDF bounds 处理关节限位。
- [x] 增加 IK 结果位置误差和姿态误差计算。
- [x] 不可达、非有限输入和不合格结果输出明确状态，并保持上一安全命令。
- [x] 使用数值雅可比条件数检测接近奇异位形。
- [x] 输出关节限位裕量并拒绝低于安全阈值的结果。
- [x] 统计纯 IK 求解耗时和后验校验耗时。
- [x] 建立可复现 20ms 基准工具。

待完成：

- [ ] 增加多解采样与评分。
- [ ] 评分项包含：当前关节连续性、限位裕量、碰撞风险、可执行性。
- [ ] 将难姿态冷启动最坏耗时从当前 `32.935ms` 优化到 20ms 内。

当前基准：

- 2026-07-22 Ubuntu VM，连续跟踪 80 个样本：P95 `7.883ms`，最大
  `10.332ms`，20ms 内比例 `100%`。
- 从零初值直接求难姿态：最大 `32.935ms`，尚不满足全场景 20ms。

验收标准：

- 对可达目标输出可执行关节解和误差。
- 对不可达、奇异、碰撞风险目标输出失败状态和原因。
- 典型工况 IK 耗时满足项目确认指标。

## 3. 连续轨迹生成与中断归零

状态：`[~]`

已完成：

- [x] 关节空间平滑插值轨迹。
- [x] 任意演示姿态到回零。
- [x] 任意演示姿态到左臂归零、右臂握手起手式。
- [x] 基于当前 joint state 作为轨迹起点。
- [x] 使用正式 `trajectory_msgs/JointTrajectory` 输出规划结果。
- [x] 增加速度、加速度和控制周期约束。
- [x] 使用 ROS2 Action 支持只规划、执行和执行中 cancel。
- [x] Cancel 后优先读取停止时的最新关节状态并规划返回安全位。

待完成：

- [ ] 明确安全预备位由甲方确认并写入配置。
- [ ] 将机器人业务停止 Topic 接入 Action cancel。
- [ ] 对输出轨迹逐点执行关节限位和自碰撞检查。

验收标准：

- 上层可中断动作。
- 任意中断点可重新规划回安全预备位。
- 轨迹满足位置、速度、加速度、控制周期约束。

## 4. 自碰撞保护策略

状态：`[ ]`

已完成：

- [x] 原型中有左右 TCP 最小距离保护。

待完成：

- [ ] 建立躯干、头部、左右上臂、左右前臂、腕部的简化碰撞代理体。
- [ ] 实现胶囊体 / 球体 / 盒体距离检查。
- [ ] 输出碰撞状态、风险部位和不可执行原因。
- [ ] 将碰撞风险接入 IK 选解评分。
- [ ] 将碰撞风险接入轨迹采样检查。
- [ ] 结合甲方现场设备确认安全边界和代理体参数。

验收标准：

- 机械臂与躯干、头部、对侧机械臂主要碰撞风险可实时检测。
- 碰撞或近碰撞目标不会输出可执行轨迹。

## 5. ROS2 接口封装

状态：`[~]`

已完成：

- [x] 输入 topic：`arm_ik/left_target`、`arm_ik/right_target`。
- [x] 输出 topic：`pico_control_joint_commands`。
- [x] Demo 服务：`go_home`、`go_handshake_ready`、`go_demo_pose_a`、`go_demo_pose_b`。
- [x] 原型状态 topic `arm_ik/status` 输出误差、耗时、奇异性、限位裕量和失败原因。
- [x] 正式 `SolveArmIK` Service 接口包和节点实现。
- [x] Service 请求包含型号、左右臂、当前关节、6D 位姿、TCP 和校验约束。
- [x] Service 响应包含状态、关节解、误差、耗时、奇异性、限位和碰撞能力状态。
- [x] 正式 `PlanArmTrajectory` Action，支持规划、执行、反馈和 cancel。

待完成：

- [ ] 接入 `withoutarm.onnx` state 的最终控制输出合并点。
- [ ] 碰撞引擎完成后将真实碰撞结果填入正式接口。

验收标准：

- 上层控制系统可通过稳定接口调用 IK / 轨迹规划。
- 每次调用均有结构化状态和失败原因。

## 6. 可视化评审 Demo

状态：`[~]`

已完成：

- [x] RViz 加载 URDF 模型。
- [x] 循环展示任意姿态到回零。
- [x] 循环展示任意姿态到握手起手式。
- [x] 字幕显示当前步骤。
- [x] 字幕显示服务触发到轨迹启动返回耗时。
- [x] 右臂跨身体中线到机器人左侧的 6D 握手姿态可达点展示。
- [x] 显示运动学可达/不可达采样点、最远可达边界和当前目标。
- [x] 跨中线逐点动作字幕显示真实 IK 求解耗时和位置误差。
- [x] Ubuntu VM 评审循环 6 个稳定跨中线目标均低于 20ms。

待完成：

- [ ] 型号选择。
- [ ] 左右臂选择。
- [ ] 拖动或交互设置末端目标位姿。
- [ ] 展示 IK 位置误差和姿态误差。
- [ ] 展示真实单次 IK 求解耗时。
- [ ] 展示碰撞状态和风险部位。
- [ ] 完成碰撞代理体现场标定后，将运动学边界升级为碰撞安全可达边界。
- [ ] 演示外部中断停止和返回安全位。

验收标准：

- 客户能在本地选择型号、手臂、目标位姿，并看到完整 IK / 碰撞 / 轨迹状态。

## 7. 多型号适配

状态：`[ ]`

已完成：

- [x] 当前 ELF3 基准模型原型适配。

待完成：

- [ ] 获取新增型号 1 的正式名称、URDF、mesh、关节配置、TCP 配置。
- [ ] 获取新增型号 2 的正式名称、URDF、mesh、关节配置、TCP 配置。
- [ ] 新增型号 1 运动链适配。
- [ ] 新增型号 2 运动链适配。
- [ ] 新增型号 1 碰撞代理体适配。
- [ ] 新增型号 2 碰撞代理体适配。
- [ ] 新增型号 1 ROS2 配置和典型工况测试。
- [ ] 新增型号 2 ROS2 配置和典型工况测试。
- [ ] 输出多型号适配说明。

验收标准：

- 基准型号 + 2 个新增型号均通过同一接口调用。
- 每个型号有独立配置、测试结果和适配说明。

## 8. 测试验证与交付文档

状态：`[~]`

已完成：

- [x] 基础数学工具单元测试。
- [x] Ubuntu VM 上 `colcon build` 验证。
- [x] RViz Demo 人工验证。
- [x] 初版部署与使用说明。
- [x] IK 可达、不可达、非有限输入和数学工具自动测试。
- [x] 误差与耗时基准脚本 `tools/benchmark_ik.py`。
- [x] ROS2 输入到结构化状态输出的冒烟测试 `tools/ros_ik_smoke_test.py`。
- [x] `SolveArmIK` 正常与异常响应端到端测试。
- [x] `PlanArmTrajectory` 约束、cancel 和安全返回端到端测试。

待完成：

- [ ] 功能测试用例。
- [ ] 典型工况测试用例。
- [ ] 边界与异常测试用例。
- [ ] 多型号回归测试。
- [ ] 自碰撞状态测试。
- [ ] 可复现测试方法。
- [ ] 测试报告。
- [ ] 正式接口说明。
- [ ] 正式部署与使用文档。

验收标准：

- 测试可以在指定 Ubuntu/ROS2 环境复现。
- 交付文档能支持甲方独立部署、调用和验收。

## 当前执行顺序

1. `[已完成]` 建立正式模型配置目录和基准 ELF3 模型配置。
2. `[已完成]` 补 IK 结果误差、失败状态、奇异检测和真实 IK 耗时。
3. `[已完成]` 定义并验证正式 ROS2 Service / Action 接口。
4. `[进行中]` 增加简化碰撞代理体和碰撞状态输出。
5. `[部分完成]` Action 中断和自动安全返回已完成，等待甲方确认安全位和业务停止 Topic。
6. `[待办]` 接入 `withoutarm.onnx` state 控制链路。
7. `[待办]` 等甲方提供 2 个新增型号后做多型号适配。
8. `[待办]` 补测试报告和正式交付文档。
